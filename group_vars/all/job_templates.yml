---
controller_templates:
  - name: "BreakGlass - Compliance Check"
    job_type: "run"
    inventory: "{{ demo_inventory_name }}"
    project: "{{ demo_project_name }}"
    playbook: "playbooks/check_breakglass_compliance.yml"
    credentials:
      - "RHEL"
    labels: 
      - "{{ demo_job_templates_tag }}"
    #execution_environment: "{{ aap_ee }}"

  - name: "BreakGlass - Remediation"
    job_type: "run"
    inventory: "{{ demo_inventory_name }}"
    project: "{{ demo_project_name }}"
    playbook: "playbooks/remediate_breakglass.yml"
    credentials:
      - "RHEL"
    labels: 
      - "{{ demo_job_templates_tag }}"
    #execution_environment: "{{ aap_ee }}"