---
- name: Initialize compliance structure
  set_fact:
    compliance:
      host: "{{ inventory_hostname }}"
      timestamp: "{{ ansible_date_time.iso8601 }}"
      compliant: true
      checks: {}
